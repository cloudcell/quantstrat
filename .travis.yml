# Sample .travis.yml for R projects

language: r
#warnings_are_errors: true
sudo: required
     
env:
  global:
    - _R_CHECK_FORCE_SUGGESTS_=FALSE

r_packages:
  - PerformanceAnalytics

#install:     
r_github_packages:
  - cloudcello/blotter
#  - cloudcello/quantstrat
  
#before_script:
#  - Rscript -e 'install.packages(c("blotter", "quantstrat"), repos= "http://r-forge.r-project.org")'
  